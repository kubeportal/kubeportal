version: '3.3'

services:
  app:
    links:
      - api
    build:
      context: ./kubeportal-frontend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    restart: always

  api:
    build:
      context: ./kubeportal-api
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    restart: always